import type { IIngredient } from "../../models/ingredient";
import { IngredientName } from "../../models/ingredientName";
import image from "./image.webp";

const moleworm: IIngredient = {
    name: IngredientName.Moleworm,
    image,
}

export default moleworm;